// Main Static JavaScript for Junior Coders Homepage
// Scrolls to calendar section when CTA buttons are clicked

function scrollToCalendar() {
    // This function would integrate with Cal.com or another booking service
    // For now, it shows a message
    const message = 'Booking feature would be integrated with Cal.com here.';
    console.log(message);
    
    // In production, this would:
    // 1. Open a modal with booking form
    // 2. Initialize Cal.com embed
    // 3. Handle booking confirmation
}

// Smooth scroll fallback for older browsers
document.addEventListener('DOMContentLoaded', function() {
    console.log('Junior Coders homepage loaded successfully');
});
